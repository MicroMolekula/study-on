{% extends 'base.html.twig' %}

{% block title %}Профиль/StudyOn{% endblock %}

{% block body %}

<div style="display:flex; flex-direction:column; align-items:center; gap: 12px;" class="mt-3">
    <h1>Ваш профиль</h1>
    <div><b>Email:</b> {{ email }}</div>
    <div><b>Роль:</b> {{ role }}</div>
    <div><b>Баланс:</b> {{ balance }} ₽</div>
    <div><a href="{{ path('app_transaction') }}">История транзакций</a></div>
</div>
{% endblock %}
