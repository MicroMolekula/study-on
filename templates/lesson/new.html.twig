{% extends 'base.html.twig' %}

{% block title %}Новый урок{% endblock %}

{% block body %}
    <h1>Создание нового урока</h1>

    <div class="mb-3">Курс: <a href="{{ path('app_course_show', {'id': lesson.course.id}) }}">{{ lesson.course.title }}</a></div>
    

    {{ include('lesson/_form.html.twig') }}

    <div class="mt-3 mb-3" style="width: 100%; border-bottom: 1px solid #A0A0A4;"></div>

    <a href="{{ path('app_course_show', {'id': lesson.course.id}) }}" class="btn btn-outline-primary">Назад к списку</a>
{% endblock %}
